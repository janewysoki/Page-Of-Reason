<h1> All the Books </h1>

<% @books.each do |b| %>
    <h2><%= b.title %> by <%= b.author.name %></h2>
    <%= link_to "write a review", new_book_review_path(b.id) %>
    <div>
        <p><%= b.description %> </p>
    </div>
<% end %>